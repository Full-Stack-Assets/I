{
  "name": "contractscan",
  "version": "1.0.0",
  "private": true,
  "description": "ContractScan — AI contract red-flag check. Single-file web app + Cloudflare Worker backend.",
  "scripts": {
    "validate": "node scripts/validate.js",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate && npm test",
    "dev": "npx --yes serve .",
    "calibrate": "node calibration/run.js"
  },
  "license": "UNLICENSED"
}
